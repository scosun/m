底层设计中有组的概念
1.初始化 每一次生成 都是 一个 新组
2.



1.组概念，通过id区分，自动生成，确认是否一次创建 就归为 同一组，没有第二种方式分组

2.不同组 不能自动编号，自动编号 功能 需要 拆分成， 全部自动编号，可以按 不同分组 分别编号，功能需要修改
还要实现一个  框选部分 座区 进行 自动编号，需要新开发

3.新生成一个座区，默认该座区为一个组，要实现对已经编组的座区 选中重新编组，这个怎么编呢?

4.向心方向和中心点  没看懂 还得 细看
座位和圆心的距离 不好算，特别是位移之后，计算两点之间的距离
JsRule标尺，十字线 怎么处理?

向心点管理- 数据保存在本地
1.工具箱添加 向心点列表 和 设置向心点

关于JSRuler 怎么结合到项目里面还得研究



5.座区组旋转 还得细看

角度的确认，座区组中每个座位的角度与座区图的旋转角度是相同的
座位位置的确认，座区组的中心点没有发生变化
意思都每个座位都是围绕各自的中心点旋转?


图11  向心旋转的角度关系，没看懂
图12 以左上角第一个座位为整体旋转圆心？ 不是都是座区中心点旋转吗？ 可以动态设置吗？怎么设置？
图13 以座区图中心点作为旋转中心？
这么看来 是 有时候 是按照 各自的 方块中心点自转， 有时候能又是 整块区域 一起旋转？

这个估计得从根上开始改，这些理论上可以做，但是我感觉难度很大。


6.虚拟画布 还得 细看
当鼠标拖虚线框至屏幕边缘时，则让屏幕scrollbar进行偏移，
这么设计 虚拟画布 html布局 都是问题，scrollbar的位置 也不好说，
当鼠标 到 边缘的时候 ，鼠标还能不能 往外移动还是个问题。

并且当最终完成 还得 重绘到 真是画布，还是个问题
这个估计不好弄

7.关于座区复制 粘贴
可以做，但是需要从根上设计

8.关于座区图文件
不懂这个，js 能不能读文件 还需要调研。
如果前端不读，是直接上传到服务器，在通过服务器解析 回传数据 那应该可以


7.贝塞尔曲线 前端可以画曲线，后台提供接口 生成 


8.比例嵌套, 通过固定参数生成应该可以做到


9.可以实现